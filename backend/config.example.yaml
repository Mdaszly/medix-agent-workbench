llm:
  api_key: ""
  base_url: "https://www.dmxapi.cn/v1"
  model_name: "gpt-5-mini"
  temperature: 0.3
  max_tokens: 1800

server:
  host: "127.0.0.1"
  port: 8012

rag:
  knowledge_dir: "../data/knowledge_base"
  top_k: 5

database:
  sqlite_path: "../data/runtime/medix_enterprise.db"

features:
  enable_llm: false
  enable_deep_search: true
  enable_mock_when_llm_missing: true

security:
  # Runtime environment variables override these where applicable.
  # Public deployments should restrict CORS and set shared tokens.
  cors_origins:
    - "http://127.0.0.1:5178"
    - "http://localhost:5178"
  admin_api_token: ""
  dify_tool_token: ""
